services:

  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "test"
      POSTGRES_USER: "test"
      POSTGRES_PASSWORD: "test"
    ports:
      - "5432:5432"

  mongo:
    image: mongo
    ports:
      - "27017:27017"
